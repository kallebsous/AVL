

# Como rodar o projeto

## Back-end (Java)
```sh
cd arvore-api
mvnw.cmd spring-boot:run
```
ou, se tiver Maven global:
```sh
mvn spring-boot:run
```

## Front-end (React)
```sh
cd front-arvore
npm install
npm run dev
```
